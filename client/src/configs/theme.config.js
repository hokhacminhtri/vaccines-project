import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#4d63a6',
    },
    secondary: {
      main: '#2a388f',
    },
    accent: {
      main: '#ff9900',
    },
  },
});

export default theme;
